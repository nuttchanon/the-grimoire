# Managed paths — `grimoire sync` OVERWRITES these from the template.
# Anything not listed here is project-owned and never touched.
# Format: one path (file or dir) per line, relative to .agents/. Blank lines and # comments ignored.

AGENTS.md
rules/
standards/
stack/
agents/
skills/
commands/
tooling.json

# NEVER managed (project-owned, listed for clarity only — do not uncomment):
# local/
# memory/
# backlog/
# session/
#
# codex/ — the project's knowledge base — lives at the repo ROOT (not under .agents/),
# so it falls outside every path here and is never synced. Project-owned, seeded once by
# `grimoire init` from templates/codex/. Listed for clarity only.
